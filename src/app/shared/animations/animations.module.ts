import { NgModule } from '@angular/core';
import {
  trigger,
  state,
  style,
  animate,
  transition
} from '@angular/animations';

const modules = [trigger,
  state,
  style,
  animate,
  transition];

@NgModule({
  declarations: [],
  exports: []
})
export class AnimationsModule {}
